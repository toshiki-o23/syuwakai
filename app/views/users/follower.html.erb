<%= link_to @user.name, user_path(@user) %>  フォロワー
<%= link_to 'フォロー一覧', user_following_path(@user.id), class: "btn btn-primary btn-sm" %><br />

<% if @users.present? %>
  <% @users.each do |user| %>
    <%= link_to user.name, user_path(user) %>
  <% end %>
<% else %>
  フォローされていません
<% end %>

