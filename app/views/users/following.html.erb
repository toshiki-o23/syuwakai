<%= link_to @user.name, user_path(@user) %>  フォロー
<%= link_to 'フォロワー一覧', user_follower_path(@user.id), class: "btn btn-primary btn-sm" %><br />

<% if @users.present? %>
  <% @users.each do |user| %>
    <%= link_to user.name, user_path(user) %>
  <% end %>
<% else %>
  フォローしていません
<% end %>

