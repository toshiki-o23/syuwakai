<% if user_signed_in? && current_user.id != event.user_id %>
  <% if event.already_bookmarked?(current_user) %>
    <%= link_to 'ブックマーク解除', event_bookmarks_path(event), method: :delete %>
  <% else %>
    <%= link_to 'ブックマーク追加', event_bookmarks_path(event), method: :post %>
  <% end %>
<% end %>
